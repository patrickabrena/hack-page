pipeline {
    agent any
    stages {
        stage("Build") {
            steps {
             
             sh 'sudo git clone git@github.com:patrickabrena/hack-page.git /var/www/html/hack-page'
           }
        }
    }
}

// supeer simplifed steps to attempt change directory into cd var/www/html and then git clone the hack repo into it



