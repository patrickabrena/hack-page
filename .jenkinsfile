pipeline {
    agent any

  
    stages {
        stage('Clone Repo') {
            steps {
                //clean up existing directory
                // sh "rm -rf /var/www/html/*"

                // CLone the repo
                sh "git clone https://github.com/patrickabrena/hack-page.git /var/www/html"
            }
        }

        
    }
}
//this version of the groovy script works! (05/01/2024)
